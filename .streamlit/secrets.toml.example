# Template for secrets — copy to .streamlit/secrets.toml and fill in real values
# For Streamlit Cloud: paste these into app Settings → Secrets (skip the file)

ANTHROPIC_API_KEY = "sk-ant-..."
GEMINI_API_KEY    = "AIzaSy..."

# Supabase PostgreSQL — Settings → Database → Connection string (URI mode)
DATABASE_URL      = "postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"
